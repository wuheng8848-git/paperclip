# Skill 懒加载与上下文封顶 — 评审稿（只管拍板）

**背景一句：** 同一轮评论叫醒里，**全 Skill 全文、Wake 规程与 AGENTS 长文** 往往和 **已注入的 wake 摘要** 叠在一起，token 先爆、模型还在扫重复。**045** 要把 **B1 懒加载、B2 与 wake 去重、B3 工作区说明书封顶** 落到实现；本稿只锁 **产品分叉与工程原则**，细则以 [`045` 技术设计](../技术设计/045-Skill懒加载与上下文封顶-技术设计%202026-05-17.md) 为准。**改代码原则**（字段可复用则复用、最小增量、保持可与上游合流）见 [`045` 执行单](../执行/045-Skill懒加载与上下文封顶.md) **「改代码原则」** 与技术设计 **「实现原则」**。

---

## 已定案口径（2026-05-17）

下列条目为人类当轮拍板结论，**开发以本节 + 技术设计 + 执行单为准**。

1. **适配器范围（硬边界）**  
   本单**只**修改 **`packages/adapters/cursor-local`**、**`codebuddy-local`**、**`qwen-local`**。其余适配器**一律不动**，避免无效扩散与 token/回归面浪费。

2. **B1 主路径**  
   **思路 A 优先**（控制面摘要 + TOC / 指针，不把整条 `SKILL.md` 当首帧灌入）。**思路 B**（`lazy` 元数据 + preamble 只列 key）可与 `adapter-utils` **小步叠加**，但**代码改动与验收证明仅限上述三适配器**。

3. **B2 降载绑定范围**  
   **优先**在 **`issue_commented` / 评论唤起** 链路上验证与收敛；`fallbackFetchNeeded: false` 的去重按技术设计 **逐步落实**，**不强制**首轮覆盖「所有非评论唤醒 reason」。

4. **B3 AGENTS 边界**  
   **只做 Paperclip 注入段**（含 `codebuddyMd` 等与控制面拼装相关段）的封顶/折叠。**Codex / 其它 CLI 自动发现仓库 AGENTS** 等行为**不纳入本单**（后续事务 046+ 另议）。

5. **工程基线**  
   **动工前**对当前仓库做一次 **git 提交**（以文书定案与执行单状态为界），便于回滚；**不**在本评审稿记录 commit hash（随仓库自然增长）。

---

## 本轮已表态（与「已定案口径」同步）

| 条 | 结论 | 备注 |
| --- | --- | --- |
| 1（B1 主路径） | **已定案** | **A 优先**，**B** 可叠加；**仅三适配器**落代码与测 |
| 2（B2/B3 路径收窄） | **已定案** | **评论唤起优先**；B2 按设计收窄，**不一次性扫尽所有唤醒类型** |
| 3（AGENTS 边界） | **已定案** | **仅 Paperclip 注入段**；上游 CLI 自带 AGENTS **出本单** |

**与 A1 / 041 怎么对齐（取向备忘，仍有效）：**

- **默认轻 + 少重复灌** 与 **045** 同砍一棵烟囱：重复膨胀的 **全 Skill / 长规程 / 整块 AGENTS**。实现上可以 **叠在 `commentWakeTier` 低档位** 上做首包裁剪，与 **041** 验收场景共用。
- **Wake 已完整时**（`fallbackFetchNeeded: false`）**禁止** 再灌一份与 wake 同义的长规程 —— **进窗的一份以服务端 wake 渲染为准**，Skill/说明书侧只保留 **指针或短摘要**。

---

## 第 1 条白话：B1「懒加载」两路子啥差别？

（背景说明，已定案见上节。）

**A — 摘要 + TOC（偏控制面、先落地）** — **本期优先。**

**B — `lazy` 元数据** — **可与 A 叠加以契约化「只列 key」**；**仅三适配器**验证。

**你选哪条为主（或 A 本期、B 下期）：** **已定案：A 优先，B 可增量叠加；代码仅限 Cursor / Codebuddy / Qwen。**

---

## 第 2 条白话：降载只绑「评论叫醒」还是绑「不必补拉 API」的所有心跳？

**已定案：评论唤起路径优先**；其余唤醒类型后续按需扩，本单不一次性改全矩阵。

**你选哪边：** **已填入定案口径第 3 条。**

---

## 第 3 条白话：AGENTS 只砍 Paperclip 注入，还是连 Codex 自动发现一起管？

**已定案：仅 Paperclip 注入段**；CLI 自带 AGENTS 发现 **046+**。

**你选哪边：** **已填入定案口径第 4 条。**

---

## 1. B1 主路径

**A / B / 分阶段（先 A 后 B）** —— **已定案：先 A，B 按需叠加；三适配器。**

---

## 2. B2/B3 降载绑定的唤醒范围

**仅评论 / 全部 fallbackFetchNeeded=false** —— **已定案：评论唤起优先，余者后续迭代。**

---

## 3. AGENTS 收口边界

**仅 Paperclip 注入 / 含上游 CLI** —— **已定案：仅 Paperclip 注入段。**

---

## 结案表（拍板后可抄到执行单 / 技术设计「已定案」）

| 条目 | 你选哪边 |
| --- | --- |
| 1 B1 主路径 | ✅ **A 优先，B 可叠；仅三适配器** |
| 2 唤醒范围收窄 | ✅ **评论唤起优先** |
| 3 AGENTS 边界 | ✅ **仅 Paperclip 注入** |
| 总括（无开口写此处） | **已定案口径**见本文 **§已定案口径**；执行单 `status: 进行中` |

**评审状态：** ✅ **已结案**（日期：**2026-05-17**）

**跟进：** 实现与测试索引回写 [`045` 执行单](../执行/045-Skill懒加载与上下文封顶.md)；契约以 [`045` 技术设计](../技术设计/045-Skill懒加载与上下文封顶-技术设计%202026-05-17.md) + 本稿 **已定案口径** 为准。

---

**附：** 现状见 [`025` 探查](../探查/025-探查-045-Skill懒加载与上下文封顶-现状与落点.md)；母本 **§B1–B3** 见 [`24`](../长期需求/24%20评论唤起过载与编排分层改版计划%202026-05-17.md)。评审体例对齐 [`26 A1评论唤起档位-评审稿`](26%20A1评论唤起档位-评审稿%202026-05-17.md)。

# 020 — 探查：ROU-44（滞留回收）与 ROU-41 收口链

**状态：** 进行中（已补齐 **ROU-41**、**ROU-28/29** API 链与发起者；回收机制根因仍待代码侧对齐）

**返回：** [`README.md`](README.md)

---

## 0. 术语纠错（防漂移）

| 说法 | 正确定义 |
| --- | --- |
| **ROU-28 / ROU-29** | **Paperclip 实例里的工单号**（市场调研 / 竞品分析），见 **`GET /api/issues/ROU-28`**、**`ROU-29`**。 |
| **`028` / `029` 执行单** | **本仓库** `docs/项目计划/执行/` 下的 **成本页汉化**、**设置页汉化** 任务 —— **与 ROU-28/29 编号无关**，勿混谈。 |

下文凡写「28/29」均指 **ROU-**，除非显式写「执行单」。

---

## 1. ROU-41（主轴工单）— API 固化事实

以下取自 **`GET /api/issues/ROU-41`**（本地实例；数据以后台为准）。

### 1.1 标识与状态

| 字段 | 值 |
| --- | --- |
| `identifier` | `ROU-41` |
| `title` | `Product Positioning Draft: synthesize ROU-28 and ROU-29 into product thesis` |
| `status` | `done` |
| `originKind` | `manual` |
| `parentId` | 对应 **ROU-40**（父工单链一节见下） |
| `executionPolicy` / `executionState` | **均为 `null`** |

### 1.2 业务语义（照正文摘要）

- **目标**：把 **ROU-28（市场调研）** 与 **ROU-29（竞品分析）** 收成一份 **产品定位 Markdown**（人设、价值主张、功能分层、定价与 GTM 等）。
- **交付物**：issue 文档 **`product-positioning-draft`**（API `documentSummaries`：`key` = `product-positioning-draft`，`title` = `Product Positioning Draft`）。
- **正文内显式引用**：`referencedIssueIdentifiers` 含 **`ROU-42`、`ROU-28`、`ROU-29`、`ROU-43`**。

### 1.3 父链（`ancestors` 摘要）

- 直接父：**ROU-40** — *CEO Strategic Reassessment: redirect company after board cleanup*，`done`。  
- ROU-40 描述中复盘：**ROU-28、ROU-29、ROU-31** 已完成等信息（字段内兼有 **乱码片段**，见 §3 第 4 条）。

### 1.4 关联图（`relatedWork`，摘要）

**Outbound（本单指向谁）**

| 工单 | 角色 |
| --- | --- |
| **ROU-28** | 调研结论来源（title/description/文档提及） |
| **ROU-29** | 竞品结论来源（同上） |
| **ROU-42** | 深度技术类后续（文档与多条 comment 提及） |
| **ROU-43** | *Recover missing next step ROU-42*，`done` —— **42 的回收单**，与 41/44 对称的一类 |

**Inbound（谁指向本单）**

| 工单 | 备注 |
| --- | --- |
| **ROU-40** | comment 提及 `ROU-41` |
| **ROU-44** | *Recover missing next step ROU-41*，`done` |
| **ROU-57** | 标题同为 *Recover missing next step ROU-41*，`done` —— **与 ROU-44 并列的另一条回收单**，待分清是重复触发还是不同原因 |
| **ROU-58** | comment 提及 `ROU-41`（进行中工单） |

### 1.5 谁「发起」了 ROU-41（创建者 vs 经办 vs 系统）

**以当前实例 API 为准（避免把 41 当成 GLM5.0「自己开单」或当成 recovery 系统开单）：**

| 问题 | 字段 | 观测结论 |
| --- | --- | --- |
| 是不是 **系统生成的滞留回收单**？ | `originKind` | **`manual`** —— **不是** `stranded_issue_recovery`（44 那类才会走 recovery `originKind`，见 `packages/shared` 的 `ISSUE_ORIGIN_KINDS`）。 |
| **创建者**是谁？ | `createdByAgentId` | **`60b56d17-…`** → 智能体 **`CEO-CodeBuddy-DP4P`**，`role` = **`ceo`**，`adapterType` = `codebuddy_local`（实例展示名可能因编码显示异常，以 `name`/`urlKey` 为准）。 |
| **人类看板用户**创建？ | `createdByUserId` | **`null`** —— API 未挂董事会人类用户_id。 |
| **谁做事（经办）**？ | `assigneeAgentId` | **`fffac39c-…`** → **`研发-Qwen-GLM5.0`**（`qwen_local` / `glm-5`）—— **调研写结论的是这位**，但不是「建单者」。 |

**口语对齐：**「CEO 生成的」在数据面上 = **CEO 角色的 Agent 落库为 `createdByAgentId`**，不等于「真人董事长」；也不是 GLM5.0 自己建 parent issue，除非另有证据表明 CEO 工具链背后是同一人操作。

### 1.6 `successfulRunHandoff`（41 上观测）

- `state`: **`escalated`**
- `required`: **`false`**
- `sourceRunId` / `correctiveRunId`: **`null`**（与早期手写摘要里「带 run id」的 **44** 条目不混用；以各 issue JSON 为准）

### 1.7 ROU-28 / ROU-29：发起者、父单、目标与经办（API）

与 **§1.5** 同一 **`createdByAgentId`** 口径核对：**两条调研单均为 CEO 智能体建单**，且挂在 **公司目标 + CEO 战略复盘父单** 下 —— 支持「CEO 从目标看盘子、再拆调研工单」的叙事；**经办分配**则与「两单都交给 GLM5.0」不符。

**发起（creation）**

| 工单 | `originKind` | `createdByAgentId` | `createdByUserId` |
| --- | --- | --- | --- |
| **ROU-28** | `manual` | **`60b56d17-…`**（**CEO-CodeBuddy-DP4P**，与 **ROU-41** 同源） | **`null`** |
| **ROU-29** | `manual` | **同上** | **`null`** |

**目标与树位置**

| 字段 | ROU-28 / ROU-29 观测 |
| --- | --- |
| `goalId` | **同一** `91d0b936-…` → **`level: company`** 公司目标（实例展示标题可能乱码；语义为 token 代理 / SMB 方向） |
| `parentId` | **同一** → **ROU-27** —— *CEO Strategic Review: synthesize completed work and launch next phase*，`done` |
| **ROU-27**（父单） `createdByAgentId` | **亦为 `60b56d17-…`（CEO）—— 父单同为 CEO 发起 |

**经办（assignee）— 与 GLM5.0 的关系**

| 工单 | `assigneeAgentId`（实例解析） |
| --- | --- |
| **ROU-28** | **CTO-CodeBuddy-DP4P**（`role: cto`，`codebuddy_local`） |
| **ROU-29** | **CEO 本人**（同一 `60b56d17-…`） |

**结论：** **28 / 29 的建单人都是 CEO 智能体**；**写/market 调研主体落在 CTO 一单、CEO 自领竞品一单**。**研发-Qwen-GLM5.0** 出现在后续 **ROU-41** 的 `assigneeAgentId`，**不是** 28/29 上的经办。

---

## 2. ROU-44（41 的延伸 / 回收单）— 首轮取证

以下取自 **`GET /api/issues/ROU-44`**（首轮会话记录 + 与 41 对齐）。

| 字段 | 观测值 |
| --- | --- |
| `identifier` | `ROU-44` |
| 类型语义 | `stranded_issue_recovery` |
| 标题 | `Recover missing next step ROU-41` |
| 与 41 的关系 | **inbound** 图：自 **comment** 匹配 `ROU-41` |
| `status` | `done` |
| `executionPolicy` / `executionState` | **均为 `null`** |
| 原因类线索（历史笔记） | `successful_run_missing_state`、`clear_next_step` 等 —— **须与当前 API 再对一次字段名** |
| 关联 run（历史笔记） | `sourceRunId` / `correctiveRunId` 曾有 UUID —— **以当前 `GET /api/issues/ROU-44` 为准** |

**推论（弱）**：44、57 同属「对 ROU-41 补缺下一步」的 **滞留回收** 工单族；是否应对合并复盘或归因重复唤醒，待对照 **活动日志 + runs**。

---

## 3. 待查 backlog（下一棒）

1. **代码**：`stranded_issue_recovery` **创建条件**与 `reconcileStrandedAssignedIssues` / graph liveness **精确分支**（文件 + 函数名）。  
2. **数据**：**ROU-44** 与 **ROU-57** 是否为 **同一异常两类自愈** 或 **重复创建**（时间线、`origin*`、`successful_run_missing_state` 是否一致）。  
3. **观测**：**ROU-41** 的 `successfulRunHandoff.state === escalated` 与 **44/57 done** 的先后关系（是否「移交升级」与回收闭合并存）。  
4. **编码**：ROU-40 / goal / 部分描述 **乱码** 是否与 **[015-探查-评论乱码问题真因](015-探查-评论乱码问题真因.md)** 同根。

---

## 4. 交叉引用

| 文档 | 用途 |
| --- | --- |
| [`018-探查-agent-pause-checkout-recovery链路与止损结论.md`](018-探查-agent-pause-checkout-recovery链路与止损结论.md) | pause / checkout / recovery 总链路 |
| [`012-探查-回形针任务运作机制-ROU49时间线.md`](012-探查-回形针任务运作机制-ROU49时间线.md) | 任务时间线写法示例 |
| [`019-探查-心跳定时器与服务调度逻辑.md`](019-探查-心跳定时器与服务调度逻辑.md) | 定时器与唤醒（与回收并行观测时可对照） |

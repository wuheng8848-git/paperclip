---
id: exec-035-adapter-child-env-allowlist
status: 已完成
updated: "2026-05-16"
---

# 任务 035 — 适配器子进程环境：白名单继承（执行期 Bug 修复）

**性质：** 非独立产品需求；执行其它事务时发现 **宿主 `process.env` 整表透传进适配器子进程** 会把无关密钥带进编码 CLI 上下文，按 Bug 修复收口。

**返回：** [`../README.md`](../README.md) · [`index.md`](../index.md)

---

## 1. 结论

- `runChildProcess` 改为 **仅继承可数白名单宿主 env**，并与 Board / `opts.env` 显式注入合并（后者覆盖同名键）。
- **增补白名单**须遵守 **[`../最佳实践/实践-适配器子进程环境变量白名单与增补闸门.md`](../最佳实践/实践-适配器子进程环境变量白名单与增补闸门.md)** 三条闸门。

---

## 2. 代码锚点

| 项目 | 路径 |
| --- | --- |
| 白名单与合并 | `packages/adapter-utils/src/server-utils.ts`（`pickAllowlistedInheritedEnv`、`mergeAllowlistedHostEnvWith`、`runChildProcess`） |
| 单测 | `packages/adapter-utils/src/server-utils.test.ts` |
| 各内置适配器 execute | 视需要传入 `opts.env`；以当时改动为准（如 `qwen-local`、`cursor-local` 等） |

---

## 3. 验证（可对表）

仓库根目录：

```sh
pnpm exec vitest run packages/adapter-utils/src/server-utils.test.ts
```

（完整套件见根目录 `pnpm test`。）

---

## 4. 关联文书

- **[实践 · 适配器子进程环境变量白名单与增补闸门](../最佳实践/实践-适配器子进程环境变量白名单与增补闸门.md)** — 操作者与评审口径。

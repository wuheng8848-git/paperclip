---
status: 已完成
date: 2026-05-20
closed: 2026-05-20
workspace: C:\Users\wuhen\code\paperclip-routic
---

# 060 · 2026-05-20：验证昨晚修复 + 编排/UI 首日执行

> **人话：** 今天不铺大功能。先证明「关单后运行记录是对的」，再二选一推进：**界面能看清是谁叫醒的运行**，或 **routic 编排公司跑出第一条真实事务**。

> **锚点（后续任务只查，本单勿再展开）：** **「唤起归因」人话该答什么、ROU-6 样本问题** → 只读 **[050 · 产品陈述](050-运行时间线唤起溯源与档位-UI.md#产品陈述唤起归因应回答什么2026-05-20-董事会口径)** 与本文 **§7.1–7.2**。实现/UI 另开执行单，**不要**在 060 里继续改需求。

---

## 1. 今天要达成什么

| 优先级 | 目标 | 做到哪算完成 |
| --- | --- | --- |
| **P0** | 验证近期代码修复在实例上成立 | 至少 1 次「事务完成 → 运行终态为成功」，且不是「已取消」 |
| **P1（二选一为主）** | **A** 事务页体验 + 运行清单人话（修订） | **相关工作** 汉化；**动态** 扫尾并确认 **运行账本** 够用；唤起归因 **人话** 在 **运行清单 → `/runs/:id`** 迭代（见 050 §产品陈述）；**不在聊天** 标签叠卡片 |
| **P1（二选一为主）** | **B** routic 编排 v2 冷启动 | **最小闭环** ✅（ROU-6，见 058 §六）；**058 整单不关**（TB 工程计划→issue 管道未做） |
| **P2** | 待审事务 **004** 收口 | 你拍板：关单 / 返工 / 拆子单 |
| **P2** | 工作区干净 | 无意外的临时目录；本地改动有 conscious 提交或还原 |

---

## 2. 名词对照（看懂 Board / 文档用）

| 英文 / 系统词 | 中文说法 | 说明 |
| --- | --- | --- |
| **issue** | 事务 | Board 上的一条任务（API/代码仍用 `issue`） |
| **checkout** | 签出 | 智能体独占开始干活前必须签出 |
| **heartbeat** | 心跳 | 系统唤醒智能体跑一轮的调度单位 |
| **run** | 运行 / 一次执行 | 某次心跳实际跑起来的记录 |
| **done** | 已完成 | 事务终态 |
| **succeeded** | 成功结束 | 运行正常结束（**不是** cancelled） |
| **cancelled** | 已取消 | 运行被作废；关单时不应误标成这个 |
| **effectiveTrigger** | 合并后的有效触发 | **042** 算出的「到底算谁叫醒的」 |
| **运行清单** | 侧边栏 **工作** 分组 → **运行清单**（勿写「编排注入」，见 [023](../最佳实践/023-实践-Board界面中文口径与文档对照表.md)） | 看 **运行快照**、**唤起归因** |
| **事务运行账本** | 打开某 **事务详情** 页内的运行记录区 | 与运行清单对照；第二期 050 要补 **唤起归因** |
| **stream-json** | 流式 JSON 输出 | CodeBuddy 边跑边吐日志（**059**） |
| **test-co / routic** | 测试公司 / 正式公司 | 先 **test-co** 验，再同步 **routic**（见 `AGENTS.md` §5-17） |

---

## 3. 背景（为什么今天干这些）

### 3.1 昨晚已合入、今天要验的

| 改动方向 | 对应提交主题（大意） | 你要看到的 |
| --- | --- | --- |
| 关单与运行终态 | 关单后运行结案对账，**done 标 succeeded** | 事务变「已完成」后，运行详情不是「因事务终态被取消」 |
| 编排界面文案 | 编排闸门文案独立模块 | 编排相关提示为中文、语义一致 |
| CodeBuddy 观测 | stream-json + 存活旁路 | 运行中能看到「距上次输出静默多久」 |

### 3.2 关联的长期单（今天只接力，不整包做完）

| 单号 | 名称 | 今天关系 |
| --- | --- | --- |
| [050](050-运行时间线唤起溯源与档位-UI.md) | 运行时间线唤起溯源 UI | **P1-A**：事务 **动态/相关工作** + **运行清单** 唤起归因人话（见阶段 2 选项 A） |
| [058](058-routic公司编排v2.md) | routic 编排 v2 | **P1-B** 最小闭环已证（ROU-6）；058 **进行中**（TB 映射未做） |
| [004](004-修复cursor适配器及cursor-code智能体配置.md) | Cursor 适配器待审 | **P2** 等人拍板 |
| [059](059-CodeBuddy流式日志与存活旁路观测.md) | CodeBuddy 流式日志 | **P0 验证** 时顺带看运行详情 |

---

## 4. 执行步骤（按顺序）

### 阶段 0 · 环境（约 10 分钟） — ✅ 已完成（2026-05-20）

- [x] 确认本机 Paperclip 已起（实例 **`http://127.0.0.1:4100`**，`GET /api/health` → `ok` / `0.3.1`）。
- [x] 确认工作目录：`C:\Users\wuhen\code\paperclip-routic`。
- [x] **routic** 上已跑通关键路径（**ROU-6**）；test-co 本轮未单独复测，下步可按需补。

### 阶段 1 · P0 验证（约 30～45 分钟） — ✅ 已完成（2026-05-20）

**SOP：** 通用 run 验收 checklist 见 **[024 · 运行验收与闭环判读](../最佳实践/024-实践-运行验收与闭环判读.md)**；下文为本日实例证据。

**路径：事务 → 签出 → 运行 → 完成**

1. [x] 选用事务 **ROU-6**（`de9e5300-7864-4cab-8e0d-c74b03b2e839`，翻译 `releases/v2026.318.0.md`）。
2. [x] 指派智能体 → 运行 → 结束。
3. [x] 事务标为 **已完成**（`completedAt` 2026-05-20T00:50:21Z）。
4. 打开该次 **运行详情**，核对：
   - [x] 终态 **succeeded**（`101c048a-b3b6-445a-b99f-449dca123028`），非 cancelled。
   - [x] 英文 lifecycle 句汉化（**056** + 2026-05-20 追加）— ROU-6 样本 **事件（3）** 已为中文；全路径见 [056 §2026-05-20](056-运行详情观测与编排叙事人机文案一轮-2026-05-18.md#2026-05-20-追加lifecycle-事件句全路径汉化060-探查执行)。
5. 打开 **工作** 分组 → **运行清单**（路由 **`/runs`**）→ **运行详情**（标签 **输入编排** 内），选中该运行：
   - [x] **唤起归因** 卡片可见 — 但 **内容与产品定位不符**（见 §7.1，非 P0 阻塞）。
6. 若用 CodeBuddy 跑过：
   - [x] 流式日志 / **距上次输出** 旁路（**059**）— **2026-05-20 补验通过**（ROU-7 · `26f74dcf-…`，见 §7 证据表）。

**回写：** run 结束前已 **POST 评论**（`0e608e2f-…`，中文完成摘要）；与 PATCH `done` 同轮完成。

**证据：** 见 §7「验证证据」表 P0 行；失败则记现象 + 事务 id + 运行 id，**不要**今天顺手开大改。

### 阶段 2 · P1 主战场（下午，二选一） — ✅ 已完成（2026-05-20）

**前置（已做，不占 P1 名额）：** 运行清单 URL **`orchestration-injection` → `/runs`**（`74f54397`）；旧书签自动重定向。

#### 选项 A — 050 相关（2026-05-20 修订：先做汉化与「动态」，暂缓聊天区唤起归因）

| 步骤 | 内容 | 状态 |
| --- | --- | --- |
| A1 | **相关工作** 标签：汉化 `IssueRelatedWorkPanel` | ✅ `5b560a45`（`issueDetailRelatedWorkPanel`） |
| A2 | **动态** 标签：扫英文漏网；确认 **运行账本** 是否已满足「看本事务关联 run」 | ✅ 子卡片汉化（`5b560a45`）+ **活动流** 二轮（`activity-format`：状态/优先级、`read_marked`、去重「添加了」前缀、董事会标签）；账本 **查看运行** 仍链智能体页 |
| A3 | run 行链 **`/runs/:id`** / **运行清单** 唤起归因人话（050 §产品陈述、§7.1） | ⏳ backlog，今日不大改 |
| A4 | 聊天区接 `effectiveTrigger` | ⏸ 暂停 |
| A5 | 智能体 **运行** 与 **运行清单** 组件统一 | ⏳ 见 050 · Run 呈现统一 |

**运行详情页壳（非 A 表内步骤，已顺手）：** 去掉页内重复「返回列表 + 标题」；顶栏面包屑末级 **`运行详情 · {runId 前 8 位}…`**（`6f39406c`）。

> **董事会口径：** 聊天区已够干净；**动态** 才是时间流+运行账本；关联 run 已有 **查看运行**（智能体运行页）。唤起归因不在事务 **聊天** 标签重做。

#### 选项 B — 058 首条闭环（推荐：要 routic 公司「活起来」）

| 步骤 | 内容 |
| --- | --- |
| B1 | 从当前工程计划 **手工写 1 条** 事务标题+描述（不走 goal/CEO 链） |
| B2 | 公司：**routic 编排 v2**（见 [058](058-routic公司编排v2.md) §六） |
| B3 | 指派 **T5 执行**（或你指定的工人），**手动 invoke** |
| B4 | 运行结束后：事务评论只留 **中文摘要 + 文档路径**，不贴整段日志 |
| B5 | 在 058 勾选「最小闭环」并记下 issue id / run id — **已勾**（ROU-6 / `101c048a-…`）；**058 不关单**（TB 融合未做） |

> **今天只选一个做主项**；另一个若有时间做「只建事务不跑」级别即可。

### 阶段 3 · P2 收口（傍晚，约 30 分钟） — ✅ 已完成（2026-05-20）

- [x] **004**：董事会关单 — 已尝试对齐 Cursor 适配器/改名，**最小冒烟未跑通**；**暂停维护 Cursor 路线**（两套适配器够用；新增优先 Codex / Claude Code）。见 [004 §3.9](004-修复cursor适配器及cursor-code智能体配置.md#39-关单决议2026-05-20--董事会)。
- [x] **工作区**：代码改动已提交；`.smart-env` 保留在 `.gitignore`；`releases/v0.2.7.md` 英译中 **未纳入本单**（后续按需）。
- [x] 已删除 `.tmp-smoke-encoding/`、`code/litellm-tb/__pycache__/`，并在 `.gitignore` 忽略同类临时项。
- [x] §7 验证证据已填；本单 `status: 已完成`，与 [`00` 主表](../00.项目任务清单.md) 同步。

---

## 5. 今天明确不做

- **045** Skill 懒加载全量实现  
- **047～048** 适配器 stdout / Windows 启动链  
- **051～054、057** 台账、契约测试、失败门禁  
- **`24` 母本「实现与需求对齐」整表**（041～053 未阶段结案前不填）  
- **058** Token Bridge **全自动**映射管道（先手工 1 条）  

---

## 6. 验收标准（关单闸门）

1. **P0** 有可复查证据：命令或 Board 截图级描述 + issue/run 标识。  
2. **P1** A 或 B **至少完成其一**；另一项若未做，在本单写明原因与下一天建议。  
3. 未在「进行中」事务上硬改指派/项目/工作区（见 `00` 规则 §4）。  
4. 本文件 `status:` 与 [`00.项目任务清单.md`](../00.项目任务清单.md) 主表一致。  

---

## 7. 验证证据（执行时填写）

| 项目 | 日期 | 公司 | issue id | run id | 结论 |
| --- | --- | --- | --- | --- | --- |
| P0 关单 vs 运行终态 | 2026-05-20 | **routic** | `de9e5300-…` **ROU-6** | `101c048a-b3b6-445a-b99f-449dca123028` | **通过**：issue `done` + run **`succeeded`**（非 cancelled）；关单结案对账路径符合昨晚合入预期 |
| P0 运行清单·唤起归因 | 2026-05-20 | routic | 同上 | 同上 | **已验·不达标**：卡片在 **输入编排** 标签内可见；文案/字段与「谁叫醒、多源合并」定位不符（§7.1） |
| P0 lifecycle 事件句 | 2026-05-20 | routic | ROU-6 | `101c048a-…` | **通过**（事件 3 条中文；服务端 `heartbeat-lifecycle-messages.ts` + UI legacy 映射） |
| P0 CodeBuddy 静默旁路 | 2026-05-20 | routic | `a01adf6a-…` **ROU-7** | `26f74dcf-0759-4cc8-9c56-35091843f2fe` | **通过**（059 补验）：`codebuddy --output-format stream-json`；运行中 API `lastOutputAt`/`lastOutputSeq` 批次推进；UI **运行记录 → 原始** 毫秒时间戳；时长 2 分 45 秒 · `succeeded` |
| P1-A 事务页 + 运行清单 | 2026-05-20 | routic | ROU-6 | — | **可关单（汉化线）**：A1+A2 完成；A3 唤起归因人话 backlog；运行详情面包屑 `6f39406c` |
| 路由 `/runs` | 2026-05-20 | — | — | — | **完成**（`feat(ui): 运行清单路由改为 /runs`） |
| P1-B 058 最小闭环 | 2026-05-20 | routic | ROU-6 | `101c048a-…` | **已实现**（issue+run+中文评论）；**058 §六「最小闭环」已勾选** |
| P1-B 058 TB 工程计划融合 | 2026-05-20 | routic | — | — | **未做**；058 保持 **进行中**，不可关单 |
| P2 004 拍板 | 2026-05-20 | routic | — | — | **关单**：查证完成；人类侧已试未通；**暂停 Cursor 适配器**，新增适配器优先 Codex/Claude Code（004 §3.9） |

**命令/检查（按需粘贴输出摘要）：**

```text
GET http://127.0.0.1:4100/api/health → status ok, version 0.3.1
GET /api/heartbeat-runs/101c048a-b3b6-445a-b99f-449dca123028 → status succeeded
GET /api/issues/de9e5300-7864-4cab-8e0d-c74b03b2e839 → status done
GET /api/issues/.../comments → 1 条中文完成评论（2026-05-20T00:50:00Z 量级）
GET /api/heartbeat-runs/26f74dcf-0759-4cc8-9c56-35091843f2fe → status succeeded, lastOutputSeq 38, logBytes 62627
pnpm exec vitest run packages/adapters/codebuddy-local/src/server/stream-liveness.test.ts → 4/4 pass
```

### 7.1 问题记录：「唤起归因」卡片内容不符合定位

**样本：** ROU-6 · run `101c048a-b3b6-445a-b99f-449dca123028` · 入口：**运行清单 → 运行详情 → 标签「输入编排」**。

**产品定位（[042](042-多源唤醒合并与有效触发归因-已完成.md) / [050](050-运行时间线唤起溯源与档位-UI.md) 第一期）：**  
让人回答——**同一短时窗多路叫醒合并后，最终算谁触发**；若有被挤掉的，列出 **被合并吸收的其他触发**。不是重复运行列表里的来源列，也不是提示词编排说明。

**界面上实际看到（与 API `contextSnapshot.effectiveTrigger` 一致）：**

| 字段 | 显示 |
| --- | --- |
| 合并后认账的触发 | **分配 · 分配交办** |
| 补充说明 | **`system`**（英文，未汉化） |
| 被合并吸收的其他触发 | **无** |
| 最后一次合并时间 | **无** |

**为何不符合定位（人话）：**

1. **没有「合并」却仍叫「合并后认账」** — `absorbed` 为空、无 `lastMergeAt`，对用户像普通指派叫醒，卡片标题误导。  
2. **答不出「谁叫醒」** — ROU-6 是人工 **invoke** 做翻译；卡片只有「分配 / 分配交办 / system」，看不出董事会手动触发、哪条评论、是否定时心跳被挤掉等。  
3. **与运行列表信息重复** — 列表已有来源/原因；单源场景下卡片几乎无增量。  
4. **位置不对** — 卡片放在 **「输入编排」** 里、紧挨 **最终提示词**，像 prompt 附属信息，不像「叫醒因果」；更合理应在 **运行快照** 旁或独立摘要区（050 第二期事务页亦同理）。  
5. **补充说明未走人机文案** — `triggerDetail: system` 原样露出，与 [056](056-运行详情观测与编排叙事人机文案一轮-2026-05-18.md) 方向不一致。

**API 取证（便于后续改 UI/服务端文案）：**

```json
"effectiveTrigger": {
  "v": 1,
  "absorbed": [],
  "winningInvocation": {
    "reason": "issue_assigned",
    "source": "assignment",
    "triggerDetail": "system"
  }
}
```

**处置建议（记入 backlog，今日不在 060 开大改）：**

| 优先级 | 方向 |
| --- | --- |
| P1 | **单源降级展示**：无 `absorbed` 时改标题/文案（如「本次叫醒」），并串联 **invoke 操作者**、**wake 评论 id**（若 `contextSnapshot` 有） |
| P1 | **换区**：唤起归因挪出「输入编排」，贴近 **运行快照** 或运行详情首屏 |
| P2 | **汉化** `triggerDetail` 枚举（`system` → 系统派工 等） |
| P2 | 050 **第二期** 事务 **事务运行账本** 复用同一套人话组件时再统一 |

**关联：** [050](050-运行时间线唤起溯源与档位-UI.md) · [042](042-多源唤醒合并与有效触发归因-已完成.md) · [023](../最佳实践/023-实践-Board界面中文口径与文档对照表.md)

### 7.2 产品复述：唤起归因应有一句明确说法（2026-05-20）

董事会口径（与 [050 §产品陈述](050-运行时间线唤起溯源与档位-UI.md#产品陈述唤起归因应回答什么2026-05-20-董事会口径) 同步）：

1. **主因：** 这次 run 是 **评论叫醒**、**分配交办**、**定时**、**手动按需** 还是别的。  
2. **若分配：** 是 **人类（董事会）**、**某智能体** 还是 **系统规则** 触发的指派——不能只有一个 `system`。  
3. **若分配：** **为什么派**（建单、改经办、接力、恢复…）。  
4. **若评论：** **哪条评论**（可点开）。

042 原始设计侧重 **多路合并谁赢**；上面四条是 **every run 的人话归因**。ROU-6 快照里已有 `source: issue.create`、`wakeTriggerDetail` 等，**唤起归因卡片未接**，故体感「不像归因」。

---

## 8. 链回

| 文档 | 用途 |
| --- | --- |
| [050 唤起溯源 UI](050-运行时间线唤起溯源与档位-UI.md) | P1-A |
| [058 routic 编排 v2](058-routic公司编排v2.md) | P1-B |
| [056 人机文案一轮](056-运行详情观测与编排叙事人机文案一轮-2026-05-18.md) | P0 汉化预期 |
| [059 CodeBuddy 流式](059-CodeBuddy流式日志与存活旁路观测.md) | P0 旁路 |
| [004 Cursor 待审](004-修复cursor适配器及cursor-code智能体配置.md) | P2 |
| [01 需求说明](../01.项目需求说明.md) | 产品口径、残留风险 |

---

## 9. 当日交付统计（2026-05-20 · 至约 10:30）

> 口径：本仓库 `master` 上 **2026-05-19 午后～20 日上午** 与 060 相关的提交与文档；不含未提交脏目录（`__pycache__`、`.tmp-smoke-encoding`）。

| 类别 | 件数 | 代表提交 / 说明 |
| --- | --- | --- |
| **P0 产品修复** | 2 | `d483689a` 关单→run succeeded；`de110106` 编排闸门文案模块 |
| **P0 观测** | 1 | `95c82b6f` CodeBuddy stream-json（059）；**2026-05-20 补验** ROU-7 / `26f74dcf-…` 通过 |
| **P0 验证** | — | ROU-6 + run `101c048a` succeeded + lifecycle 中文 |
| **路由 / 运行清单** | 1 | `74f54397` `/runs` |
| **lifecycle 汉化** | 1 | `9ff79d96` 服务端 + UI legacy |
| **事务详情三标签** | 3 | `5b560a45`、`68f7db60` 活动流、`6f39406c` 面包屑 |
| **文档 / 口径** | 5+ | `6dc77fdb`、`a64fa5ef`、`75e01969`、`dc2f5338`、050/056/060 同步 |
| **实例** | — | 4100 停服→`dev:nuke`→`dev:once` 重启 |

**060 闸门自检（收口 · 2026-05-20）：**

| 闸门 | 状态 |
| --- | --- |
| P0 证据 | ✅ |
| P1 A 或 B | ✅ **A（汉化线）** + **B 最小闭环**（ROU-6）；A3 唤起归因 → 050 backlog |
| P2 004 | ✅ 关单（004 §3.9） |
| P2 工作区 | ✅ |
| **本单** | ✅ **已完成**（058 / 050 后续项不在 060 范围内） |

---

## 修改记录

| 日期 | 摘要 |
| --- | --- |
| 2026-05-20 | 文首 **锚点**：唤起归因只读 050 §产品陈述 + 060 §7.1–7.2；本单不再展开 |
| 2026-05-20 | §7.2：董事会四要素人话归因；050 增「产品陈述」节 |
| 2026-05-20 | §7.1：**唤起归因** 卡片已验但不达标（ROU-6 / `101c048a`）；问题记入 backlog |
| 2026-05-20 | 口径对齐 [023]：运行清单 / 事务运行账本；废弃「编排注入」写法 |
| 2026-05-20 | **阶段 0–1 完成**：4100 健康；ROU-6 + run `101c048a-…` **succeeded** + 评论回写；§7 证据表已填 |
| 2026-05-20 | P0 lifecycle：056 追加全路径汉化；060 §7 证据与阶段 1 勾选 |
| 2026-05-20 | 阶段 2 标未开始；P1 表与选项 A 对齐董事会修订；`/runs` 路由已落地 |
| 2026-05-20 | 阶段 2 **进行中**：A1/A2 完成（`5b560a45`）；实例 **4100** 已停服重启；A3 唤起归因仍 backlog |
| 2026-05-20 | A2 活动流二轮：`activity-format` 状态/优先级汉化、`read_marked` 等动作、修复「添加了」重复前缀 |
| 2026-05-20 | §9 当日交付统计（至约 10:30） |
| 2026-05-20 | P2：004 董事会关单（暂停 Cursor 适配器；§3.9） |
| 2026-05-20 | 058：最小闭环勾选完成；TB 工程计划→issue 管道未做，058 仍 **进行中** |
| 2026-05-20 | 由当日工作计划落盘为执行单 **060** |
| 2026-05-20 | **收口**：阶段 2–3 完成；`status: 已完成`；§6 闸门全部满足 |

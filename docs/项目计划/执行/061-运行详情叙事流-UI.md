---
status: 待办（预览已定稿）
date: 2026-05-20
---

# 061 · 运行详情叙事流（UI）

> **人话：** 运行详情别再用五个 Tab 拼碎片。首屏 **概览** 讲结论（终态、元信息、Token、结构化回传）；下面三段叙事讲 **来源 → 输入编排 → 执行与回传**；全文 prompt、转写、JSON **从段内或概览「技术数据」点开**。

**设计真值：** **[`技术设计/054-运行详情叙事流-技术设计 2026-05-20.md`](../技术设计/054-运行详情叙事流-技术设计%202026-05-20.md)**（已定案 · **§2.1 界面文案**：中文产品说明，非口语、非裸英文术语）

**UI 定型稿（dev 预览 · 未接 API）：** `ui/src/pages/RunDetailNarrativePreview.tsx`  
**打开：** `http://localhost:4100/{公司前缀}/design-preview/run-detail-narrative`（例：`ROU/design-preview/run-detail-narrative`）

**静态入口（跳转说明）：** [`技术设计/previews/061-运行详情叙事流-静态预览.html`](../技术设计/previews/061-运行详情叙事流-静态预览.html)

**需求输入：** [060 §7.1–7.2](060-2026-05-20-验证闭环与编排首日执行.md) · [050 §产品陈述](050-运行时间线唤起溯源与档位-UI.md#产品陈述唤起归因应回答什么2026-05-20-董事会口径)

**取代呈现：** [054](054-运行原子诊断四标签与信息架构.md) 的 **五 Tab 主导航**（054 语义分层仍有效，见设计 §9）

**Git 预览定型：** `19318766` 起设计文档 + dev 路由；预览 UI 定型稿见最新 `RunDetailNarrativePreview.tsx` commit。

---

## 1. 定型稿 IA（2026-05-20 董事会确认）

| 区域 | 锚点 | 内容 |
| --- | --- | --- |
| **概览** | `#outcome` | 标题栏（略大 · 图标 · 副标题底对齐）+ **八格指标**（智能体/适配器/时长/退出码/状态/Token×3）+ 结构化回传摘要 + 「技术数据 ▾」（JSON 取证）+ 复制运行 ID |
| **运行来源** | `#source` | 050 四要素叙事 + 关联链 + 段内操作 |
| **输入编排** | `#input` | 提示词段清单 + cache 行 + 「查看最终提示词全文」 |
| **执行与回传** | `#execution` | 命令参数表 + 转写预览 + 友好/原始/完整命令行 |

**明确不做（相对初版 054）：**

- ~~独立第四段「结果」叙事卡~~ — 终态与用量已并入 **概览**
- ~~顶栏「深读 ▾」全菜单~~ — 深读从 **段内 footer** 触发；JSON 收进概览 **「技术数据」**
- 概览内不重复 run ID 短行（面包屑 + 复制运行 ID 已够）

**预览组件（M1 可抽共享）：** `OverviewCard` · `NarrativeCard` · `MetricStrip` · `StatCell` · `PropertyRow` · 右侧 Sheet（`?expert=` 待 M1 接 URL）

---

## 2. 范围

| 在范围内 | 不在范围内 |
| --- | --- |
| `/runs/:runId` 概览 + 三段叙事 + 段内/技术数据深读 | 事务详情「动态/聊天」二期（050 第二期） |
| §1 来源（050 四要素） | 新 API / 服务端字段（除非实现时发现真缺口另开单） |
| §2 提示词段清单（非全文重复） | 045 skill minimize 全量实现 |
| 概览八格 + 结构化回传摘要 | M4 智能体页完全统一（可分期） |
| M1–M2 交付 | |

---

## 3. 交付分期（对齐定型稿）

### M1 — 骨架、真数据、深读接线

- [ ] 去掉五 Tab 主路径；落地 `#outcome`（概览）`#source` `#input` `#execution`
- [ ] 从 `RunDetailNarrativePreview` 抽组件，替换 `OrchestrationInjectionRunDetail` 主路径
- [ ] 接 `heartbeatsApi.get` / 现有 `contextSnapshot` · `promptSections` · `adapter.invoke` · `resultJson` 解析
- [ ] 概览：运行态着色（`runStatusText`）、运行中时长/Token 占位规则
- [ ] 段内 Sheet 深读：`prompt` · `transcript-friendly` · `transcript-raw` · `json-invoke`
- [ ] 概览「技术数据」：`json-context` · `json-wake` · `json-result`
- [ ] 旧 `?tab=` URL 重定向或锚点 / `?expert=` 映射
- [ ] i18n：`runDetailOverview*` · 概览/段标题文案

### M2 — 调编排可读

- [ ] §1：`run-source-narrative` 单测 + ROU-6 单源降级文案（050 四要素）
- [ ] §2：`prompt-blocks` 段清单 + `promptCacheCorrelation` 一行；块标签 i18n（中文（id））
- [ ] §3：CodeBuddy **命令参数表**（catalog 试点）+ 运行中静默旁路（059）
- [ ] 移除概览/段内重复 `finalPrompt` Markdown 大块

### M3 — 拆嵌套（后续）

- [ ] 从 `HeartbeatRunDetailPanel` 抽转写/操作；运行记录 Tab 不再嵌整页

### M4 — 智能体页对齐（后续）

- [ ] `/agents/:id/runs/:runId` 复用 section 组件

---

## 4. 验收（M1+M2）

1. 打开 ROU-6 或等价 succeeded run：**不滚动**可读概览八格 + 结构化回传摘要；下滚三段叙事无 Tab 切换。
2. 深读：§2 最终提示词、§3 友好/原始/命令行、概览技术数据 JSON 均可打开 Sheet 且内容为真数据。
3. 设计 §2.1 文案纪律：概览/段内无裸 `resultJson` / `adapterConfig` 等作主文案。
4. `pnpm exec vitest run ui/src/lib/run-detail/`（新建单测路径）通过。
5. 旧 `?tab=finalPrompt` 等可重定向或锚点到等价位置。

---

## 5. 验证证据

| 项 | 状态 |
| --- | --- |
| dev 预览定型稿 | ✅ `RunDetailNarrativePreview.tsx`（mock 数据，未接 API） |
| 现网 `/runs/:runId` | ❌ 仍为五 Tab（`OrchestrationInjectionRunDetail.tsx`） |
| M1 实例验收 | （实现后填：run id / 截图 / commit） |

---

## 修改记录

| 日期 | 摘要 |
| --- | --- |
| 2026-05-20 | 自 060 后续 + 董事会方案 A 定案落单 |
| 2026-05-20 | **预览定型稿**：概览八格、结果并入概要、去顶栏深读、段内深读 + 技术数据 JSON；更新 M1 分期与验收 |

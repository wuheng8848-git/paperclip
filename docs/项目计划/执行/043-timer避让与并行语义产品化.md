---
status: 待办
---

# timer 避让与并行语义产品化（maxConcurrentRuns）

**母本：** [`../长期需求/24 评论唤起过载与编排分层改版计划 2026-05-17.md`](../长期需求/24%20评论唤起过载与编排分层改版计划%202026-05-17.md) · **§A3**、§1 目标(1)(3)

## 背景与范围

将 `queued` / `running` / `heartbeat_timer` / `automation` 与 **`maxConcurrentRuns`** 的组合写清为**产品与 UI 可见语义**，并提供可选策略（如：processing comment wake 时 timer **退让 N 秒**或丢弃本轮 timer）。

## 交付物（可验收）

1. UI 文案 + 开发者文档（可与 **033**、[最佳实践/004](../../最佳实践/004-实践-工单心跳与僵尸run排障.md) 交叉）。
2. 可配置或可代码固定的退让规则（若无配置则写明默认）。
3. 用户可复现实验：**并发=1** 时 queued + timer 同框的预期行为截图或日志。

## 依赖与并行

- **耦合**：[`042-多源唤醒合并与有效触发归因-已完成.md`](042-多源唤醒合并与有效触发归因-已完成.md)。
- **参考**：[`007-心跳调度无issue不触发.md`](007-心跳调度无issue不触发.md)、[`032-agent-pause止损与heartbeat释放一致性.md`](032-agent-pause止损与heartbeat释放一致性.md)。

## 验证证据（完成后填写）

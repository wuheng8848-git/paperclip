---
id: exec-003-activity-review
status: 已完成
ledger: ./任务执行台账.md
feeds:
  - ./001-适配器修复.md
  - ./002-智能体技能挂载.md
  - ./025-如何让AI快速拿到配置信息.md
  - ./026-如何快速测试.md
updated: "2004-05-13"
scope: readonly
closed: "2004-05-13"
close_note: 未续做的事件表深挖/日志穷尽检索归入 001、002（002 已关拆 025/026，继任单见 feeds）。
---

# 任务 003 — 查看昨天的活动记录

**返回：**[`任务执行台账.md`](任务执行台账.md)

**范围：** **只读、只汇总**；不向 Paperclip / 适配器 / 仓库做任何写操作。产出供 **[001 — 适配器修复](001-适配器修复.md)** 与 **[002 — 智能体技能挂载](002-智能体技能挂载.md)**（已关闭；继任 **[025](025-如何让AI快速拿到配置信息.md)** / **[026](026-如何快速测试.md)**）引用。

**目标昨日：** **2004-05-12（北京时间整日）**。UTC 查询窗：`2004-05-11T16:00:00.000Z`～`2004-05-12T15:59:59.999Z`。

**编号勘误（读 §2/§3 时）：** 历史正文里的 **012 / 013** 为旧草稿号；对应 **`001` / `002`** 任务单。`002` 已关闭，技能与测试继任为 **`025` / `026`**。

---

## 1. 需求

### 要什么

在不大改任何东西的前提下，**还原 2004-05-12 这一天**与工作流相关的 observable 活动：**谁在什么公司/项目上做了什么**（工单、指派、heartbeat、适配器报错、失败 run、与人类相关操作）。

目的：为 **001（适配器问题）** 与 **002（技能挂载）** 提供事实材料（时间线、报错原文、涉及到的 agent · adapter · skill key），避免凭猜测定界。

### 验收直觉

1. §3 中有一条 **可按时间阅读的摘要**（或表格），每条最好带 **可查的指针**（issue id/run id/仪表盘路径）。
2. 明确指出与 **适配器不可用 / 报错 / sync 失败** 相关的条目及其与 **012** 的映射建议。
3. 明确指出与 **技能展示、desiredSkills、公司库、挂载失败或未挂载** 相关的条目及其与 **013** 的映射建议。

### 硬性约束（只查不改）

- 禁止：**PATCH 工单状态**、改 agent 配置、公司导入导出、适配器重装、批量技能同步、在生产库 `DELETE`/`UPDATE`。
- 允许：**Dashboard / Runs / Issues / Logs** 的阅读、导出、浏览器复制、`curl GET`（只读 API）、在本机克隆上的 `git log --since`。（本次另：本机 **Postgres `heartbeat_runs` 只读 SQL**。）

---

## 2. 执行方案

> 执行完成后把要点写入 §3；**012 / 013** 文内可复制链接或小节引用。

### 2.1 建议查询面（任选能拿到的）

- [ ] Paperclip **Dashboard / Activity**（若存在）：过滤昨天。
- [ ] **Issues**：`updated`/`created` 在目标日或为该日活跃的 assignee/agent。
- [x] **Agents → Recent Runs** 等价：`heartbeat_runs` × `agents`（结构化，见 §3）。
- [ ] **Inbox / 审批**：未逐条翻阅。
- [x] **server.log**：CodeBuddy/Codex/测试路径关键字抽样（见 §3.5）。
- [ ] Cursor / 本会话：**不替代**站内记录。

### 2.2 结构化输出模板（可复制到 §3）

对每条值得关注的事件尽量包含：

`| 时刻(约) | 类型 | entity | 摘录/错误信息 | → 服务于 012/013 |`

### 2.3 与下游任务对齐

- [x] §3 **→ 001**。
- [x] §3 **→ 002**。

---

## 3. 执行回写

> **本节为已落实的结论。**

### 3.1 元信息

| 字段 | 内容 |
|------|------|
| 汇总日期（实际查阅窗口） | 北京 **2004-05-12** 整日；等同 UTC `2004-05-11T16Z`～`2004-05-12T15:59:59.999Z` |
| 查阅人 | （探查会话执行者填） |
| Paperclip 实例 | 默认实例：`C:\Users\wuhen\.paperclip\instances\default`；嵌入式库端口 **54329**；连接串形 `postgres://paperclip:paperclip@127.0.0.1:54329/paperclip` |

### 3.2 活动摘要（主体）

**`heartbeat_runs` 单日状态**

| status | n |
|--------|--:|
| failed | 21 |
| succeeded | 20 |
| cancelled | 3 |

**按 `adapter_type` × `status`（同日）**

| adapter_type | failed | succeeded | cancelled |
|----------------|-------:|-----------:|----------:|
| `codebuddy_local` | 12 | 8 | 0 |
| `codex_local` | 9 | 3 | 0 |
| `cursor` | 0 | 1 | 3 |
| `process` | 0 | 8 | 0 |

**同日失败类型摘要（可查库复核）**

- `codebuddy_local`：`error_code` 多为 **`codebuddy_parse_error`**（`Failed to parse CodeBuddy JSON output`）；另有 **`process_lost`**、`CodeBuddy exited with code 3221226091`（日志侧）。
- `codex_local`：**`adapter_failed`** — API 报 `gpt-5.3-codex-spark` **在 ChatGPT 账号跑 Codex 时不被支持**；日志另有 **`codex_local` 测试环境起 `sh` 在 Windows 下失败**（500）。

### 3.3 → 供 012（适配器修复）使用

| 线索 | 说明 |
|------|------|
| **CodeBuddy JSON 解析** | 大量 `codebuddy_parse_error`：stdout 非预期「JSON 数组 / result 消息」形态时即失败；代码入口：`packages/adapters/codebuddy-local/src/server/execute.ts`（`parseCodeBuddyJsonOutput` / `!resultJson` 分支）。 |
| **CodeBuddy 进程** | `process_lost`、exit code **3221226091**、`No project … fallback workspace` 类 stdout——需与工作目录、CLI 稳定性、宿主 Windows 一起看。 |
| **Codex 模型与账号** | 失败体为 OpenAI `invalid_request_error`：**`gpt-5.3-codex-spark` + ChatGPT 账号**组合；默认模型/向导是否仍推此组合需产品与文档对齐（`packages/adapters/codex-local/src/index.ts` 等）。 |
| **Codex 环境探测** | `testEnvironment` 在部分路径使用 **`sh -c`** 写 probe home（无 API key / probe 分支），Windows 宿主会报 **Failed to start command "sh"** — 与 API 报错不同类（`packages/adapters/codex-local/src/server/test.ts`）。 |

### 3.4 → 供 013（智能体技能挂载）使用

- 当日 **`heartbeat_runs` 失败样本未发现**直指技能挂载 / `skills` sync 的 `error` 文案。
- 「技能是否与失败相关」若在 013 需举证：对具体 **run.id** 下钻 **`heartbeat_run_events`** + 与时间窗对齐的技能相关日志。（本单不展开 —— 记在 013。）

### 3.5 参考资料路径 / 快照

- DB：`heartbeat_runs` + `agents`（只读聚合）。
- 日志：`C:\Users\wuhen\.paperclip\instances\default\logs\server.log`（关键字：CodeBuddy、codex、`test-environment`、`sh`）。

---

## 4. 结项

014 职责止于只读摘要；后续深挖归入 **012 / 013**。

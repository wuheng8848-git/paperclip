---
status: 进行中
---

# 运行时间线唤起溯源、预览与轻量档位（UI）

**母本 / 拆单出处：** [`../长期需求/24 评论唤起过载与编排分层改版计划 2026-05-17.md`](../长期需求/24%20评论唤起过载与编排分层改版计划%202026-05-17.md) · **§E1–E3**

## 背景与范围（人话）

- **E1**：让人在界面上能追到「这条运行到底算谁叫醒的」，最好和评论、时间线放在一眼能懂的地方（本期先做编排注入页这一站）。
- **E2**：发帖/操作前预估会拉多少规程（和 **041** 档位挂钩）——**未做**。
- **E3**：轻量叫醒 vs 满载——**未做**。

## 评审与开口选项

**无开口**（先做只读展示，不碰发帖路径）。

## 落地说明（本期切片）

- **编排注入**（`工作 → 编排注入`）：选中一条运行后，在「运行快照」卡片**上方**增加 **「唤起归因」**：
  - 若有 **`effectiveTrigger`**（**042** 写入）：用人话展示「合并后认账的触发」、补充说明、最后一次合并时间，以及「被合并吸收」的列表。
  - 若没有：一段说明，引导看列表里的「原因」和完整快照（老数据或从未合并都是正常情况）。
- **收件箱 / 工单详情时间线合一屏**：**本期未做**（仍属 E1 后半）。

## 代码与测试索引

- **展示与字典：** `ui/src/pages/OrchestrationInjection.tsx`、`ui/src/lib/i18n.ts`（`orchestrationInjectionPage.wakeAttribution*`、`wakeReasonLabels`）
- **解析（可单测）：** `ui/src/lib/wake-attribution.ts`
- **单测：** `ui/src/lib/wake-attribution.test.ts`

## 验证证据

- **日期：** 2026-05-17  
- **命令：** `pnpm exec vitest run ui/src/lib/wake-attribution.test.ts`（通过）  
- **说明：** 全 UI `tsc -b` 仓库内另有历史报错，与本次文件无关。

## 依赖与并行

- **强依赖：** [041（档位）](041-评论唤起策略分级与档位-已完成.md)、[042（归因快照）](042-多源唤醒合并与有效触发归因-已完成.md)  
- **参考：** [033](033-控制面-issue-run观测与checkout应急操作.md)

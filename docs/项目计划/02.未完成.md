# 未完成 / 待跟进项（从任务单抽条）

**主表：**[`00.项目任务清单.md`](00.项目任务清单.md)  
**说明：**下面每一条对应某张 `执行/*.md` 里仍开着的事；标「已完成」的任务若还有跟进，也单独成条，避免漏看。

---

## 一、进行中

### 001 — [`执行/001-适配器修复.md`](执行/001-适配器修复.md)

1. §2.1：补填 **Paperclip Base URL / company / 样本 agent**；完成 **本机自检**（人类配置向导前提）。
2. §2.2：**列现象**（UI 原文、失败 run、API/日志摘录）写入 §3。
3. §2.2：**根因归类**（运行时 / 产品边界 / 流程与文档）。
4. §2.3：**修复路径**（改仓库则 commit/PR；仅配置则命令/env 清单）。
5. §2.3：**技能策略**（`desiredSkills` 可走的 key 列表，或 `unsupported` 的外部 SOP）。
6. §2.4：对照 **§1.4 文档地图** 与 [`最佳实践/README.md`](最佳实践/README.md) 索引；**最小工单走通** 已不作为本单目标。
7. §2.4：若有新结论，更新研究笔记 `02-智能体-agents` 相关条目（避免与 §1.4 重复）。
8. **CodeBuddy：** **`process_lost` / Windows `3221226091` / cwd 与 fallback workspace** — 仍缺专门处理与文档。
9. §3.1：补 **完成日期、执行人、关联 PR/commit**。
10. 若主攻范围含 **qwen_local**，在 §3.1 元信息 **单列**。

---

## 二、待审

### 004 — [`执行/004-修复cursor适配器及cursor-code智能体配置.md`](执行/004-修复cursor适配器及cursor-code智能体配置.md)

1. §2：按需补 **`server.log`** 同一时间窗、**`heartbeat_run_events`**。
2. §2：人类侧核对 **`qwen` / `agent` 实际二进制**、**`CURSOR_API_KEY` 来源**（系统 env / 启动方式 / `adapter_config.env`）。
3. §3.7：执行 **`where qwen` / `where agent`**（或等价命令）并记录结果。
4. §3.7：可选 — **`server.log` 秒级对齐**（若业务方要更强证据）。
5. §3.8：人类按 §3.6 做完 **env / adapter 类型** 修正后，补 **关闭说明**（可写在 §3.1「关闭日期」或 §3.8）。
6. 闭环后：`status:` 与 **`00` 主表** 从 **待审** 迁至 **已完成** 或其它一致状态。

---

## 三、待办（执行方案未写或未动）

### 025 — [`执行/025-如何让AI快速拿到配置信息.md`](执行/025-如何让AI快速拿到配置信息.md)（由 002 拆出）

1. 写清 **§2 执行方案**：固定入口（文档 / API / 模板）、**查法**优于抄实例表。
2. **§3 回写** 证据与结论；可与 [001 §1.4](执行/001-适配器修复.md) 互链。

### 026 — [`执行/026-如何快速测试.md`](执行/026-如何快速测试.md)（由 002 拆出）

1. 写清 **§2 执行方案**：默认最短测试序、何时必须深测。
2. **§3 回写**；与 [`运维-回形针本地.md`](最佳实践/运维-回形针本地.md) 互链避免重复。

### 007 — [`执行/007-心跳调度无issue不触发.md`](执行/007-心跳调度无issue不触发.md)

1. 补充 **§3 执行方案**（设计 + 改哪些模块）。
2. 实施并写 **§4 执行回写**（证据、结论）。
3. 验收：**无已分配 issue → 不触发 run**；**有分配 → 正常触发**；UI 或日志可见「跳过」类信息（以任务单 §1 为准）。

### 010 — [`执行/010-解决agent任务执行的派单和回收问题.md`](执行/010-解决agent任务执行的派单和回收问题.md)

1. 阅读任务单已列代码路径：`heartbeat.ts`、`issue-assignment-wakeup.ts`、`agents.ts`、`run-liveness.ts`、`cursor-local/execute.ts` 等，形成 **§3 方案**。
2. **僵尸 run**：issue `done` 后 run 仍 `running` — 产品或运维规则闭环。
3. **重复 issue**：同源标题/描述双单 — 幂等或门控。
4. **裸唤醒**：无 `issueId` 的 on_demand — 拒绝或不下 run。
5. 写 **§4 执行回写** 与证据。

---

## 四、任务单已「已完成」但仍有跟进（易漏）

### 015 — [`执行/015-修复审计结论.md`](执行/015-修复审计结论.md)

1. 目视确认：**新建 4 份 AGENTS.md**、**CTO extraArgs**、**6/8 agent 绑技能** 等是否已全过时或仍需补做。

### 020 — [`执行/020-活动日志异常探查-ROU19.md`](执行/020-活动日志异常探查-ROU19.md)

1. **计费**：**Cursor Ultra limit** 类条目 — 是否需账单/套餐侧跟进。
2. **适配器**：**CodeBuddy DLL / parse** — 是否另开修复单。
3. **产品**：恢复链 **cancel → blocked** 边界交互 — 改进是否立项。

---

## 五、可选复跑（非硬性未完成）

以下已关单，但若 **换公司/换数据** 可能需要重做验证（见各任务单「下一步」原文）：

- [`014-静态盘点.md`](执行/014-静态盘点.md)  
- [`016-端到端issue结账.md`](执行/016-端到端issue结账.md)  
- [`017-僵尸run清理.md`](执行/017-僵尸run清理.md)  
- [`018-Cursor-CLI-live不退出.md`](执行/018-Cursor-CLI-live不退出.md)（结论含「未改产品代码」— 若要改产品需新单）  
- [`019-CEO当时在干什么.md`](执行/019-CEO当时在干什么.md)  
- [`021-控制台任务中英混排.md`](执行/021-控制台任务中英混排.md)  
- [`022-已完成任务反复唤醒agent-ROU21.md`](执行/022-已完成任务反复唤醒agent-ROU21.md)  

---

## 修改记录

| 日期 | 摘要 |
| --- | --- |
| 2026-05-14 | 023 文书验收闭环（任务单与 `00` 主表标已完成，本清单删除 023 段）；015 删除过时的 qwen_local「仍不支持」条，并回写任务单与 YAML |
| 2026-05-14 | 002 关单并拆 **025 / 026**；本清单删 002 段、§三 增加 025/026 |

# 项目计划（Routic 式工作方式）

本目录为**仓库内协作**（任务、探查、验收证据）；**不**按对外资料库口径维护。对外向文档见 `docs/` 其余部分及 `AGENTS.md`「`docs/` 域文书」条目。

本目录与 `01.项目需求说明.md`、`00.项目任务清单.md`（含**验收标准**）、`执行/`、`探查/`、`最佳实践/`、`长期需求/`、[`系统逻辑/`](系统逻辑/)（运行时语义索引）配套使用。

下面有一条**推荐的一般顺序**（**任务不清、容易拍脑袋动代码时**用来减返工）；**不是**「执行单只能从需求端一步一步线性生发」的硬管道。实际怎么开 `执行/` 单，常见只有三条路径，且**顺拆、反补都可以**——见下文 **「执行单从哪来」**。

**Paperclip 租户（实例侧）约定：** 公司已收敛为 **test-co（测试）+ routic（正式）** 等既定集合。**新建公司：**非经人类当轮拍板不得扩张——总则 **[`AGENTS.md`](../../AGENTS.md)** §5 第 **16** 条，Cursor **[`.cursor/rules/routic-project.mdc`](../../.cursor/rules/routic-project.mdc)**。**迁移门禁：**新适配器、模型搭配、指令词/模版等与执行平面相关的变更，须在 **`test-co` 验证闭环**后再同步到 **`routic`**（§5 第 **17** 条）。

1. **任务不清楚 → 先讨论**（与人或 Board 对齐目标、边界、成功标准；不要猜任务）。
2. **讨论仍不清楚 → 先探查**：本地 **代码 + 仓库文档（如 `doc/`、`AGENTS.md`）+ [`index.md`](index.md) 全量链接**；必要时 **上网检索**补上下文。
3. **需求落定 → 再执行**（`01` 或任务单里**已写清边界**后再大改代码/动环境；**若已按下文「执行单从哪来」先落执行单**，可把「落定」理解为该单自身写清即可，**不必**强求先改 `01`）。
4. **执行结束 → 收口边界**：拆 **下一单** 承接剩余工作，或把新共识 **记回需求**；**回写 `最佳实践/`**（可复用的路径、命令、坑）。
5. **验证结论与证据 → 写进对应 `执行/` 任务单**；**验收标准**见 `00.项目任务清单.md`；**残留风险**记在 `01.项目需求说明.md`「残留风险」节。
6. **`00.项目任务清单.md`** 顶部「状态｜任务单」**表**只列状态与链接（不写执行过程）；**验收标准**、规则与「验证证据存放处」写在同文件专节；任务标题与执行过程一律放在 `执行/` 各单内。

### 042～053 编排批次（固定套路）

主表 [`00.项目任务清单.md`](00.项目任务清单.md) 中 **`042`～`053`** 与 **`041`** 同母本：**[`长期需求/24 评论唤起过载与编排分层改版计划 2026-05-17.md`](长期需求/24%20评论唤起过载与编排分层改版计划%202026-05-17.md)**（文内 **「执行单拆分（041–053）」** 表为拆单真值；不限于 `01`）。走同一套：**探查 → 技术设计 → 评审（开口选项）→ 按复杂度 TDD → 回写执行单（含代码与测试索引）**；**本批阶段结案后须在 `24` 填「实现与需求对齐」**（见该文锚点 **`#impl-alignment-24-round`**）。结案可 **`已完成`** 后缀并修链。细则见 **[`最佳实践/016-实践-042至053编排开发收口套路.md`](最佳实践/016-实践-042至053编排开发收口套路.md)**。

### 执行单从哪来（三种路径，顺序不拘）

`执行/` 任务单在实践里通常只有下面三种**建设**来源；**不要求**总是 1→2→3，也**常反着来**（先落执行单再补需求、先摸界面再回写 `01`、探查结论长大后再拆子单等）。**主表 + 文件取号**仍按 **[「`执行/` 新建 `.md` 命名与登记」](#执行-新建-md-命名与登记)** 办，与「你先从哪条路进来」无关。

1. **从需求来**：`01` 或讨论已定边界，再新建 `执行/` 并记入 `00` 主表。  
2. **从任务单 / 探查单分拆而来**：既有 `执行/` 或 `探查/` 要拆并行、拆风险、拆验收，新作子单；母单与 `01` 可随后再对齐、再补链。  
3. **人类检查界面或临时起意**：对 UI / 实例 / 文档看一眼决定要记一笔，可先写 `执行/` + 登主表，再把关联需求、探查结论、Board 收口**后补**。

**编号（`NNN-`）只表示该单进仓库的先后**，不表示你从哪条路来、也不表示必须先写完 `01` 才能开单。

**关于 `执行/` 文件名里的三位前缀（如 `001-`、`022-`）：** 只表示**任务单进入仓库的时间线**（建档先后），**不表示业务优先级**，也不当作路线图排序；是否优先做哪一单，以讨论结论、`01.项目需求说明.md` 里的开放问题以及 Board 为准。

## `执行/` 新建 `.md` 命名与登记

**须**与 **[`00.项目任务清单.md`](00.项目任务清单.md)** 主表同步，**不要**只落文件不记表。本节与 **[「执行单从哪来」](#执行单从哪来三种路径顺序不拘)** 无关：无论需求先、分拆先还是临时起意先，**取号与登记表**都照下面做。

1. **取号**：在 **`docs/项目计划/执行/`** 下列出（或 `glob`）所有符合 **`NNN-*.md`**（三位十进制数字前缀）的文件名，取其中 **最大 NNN**，新任务单文件名为 **`(NNN+1，固定三位)-中文主题.md`**（例如已有 `026-…` 则新建 `027-…`）。  
   - **例外**：人类当轮**明确指定**另一个合法三位号且不与已有文件重名时，以当轮为准。  
2. **登记表**：在 **`00.项目任务清单.md`** 顶部「状态｜任务单」表 **新增一行**，链到该新文件；「状态」与新建任务单文首 YAML **`status:`** 一致（常用初始值为「待办」）。  
3. **前缀含义**：与本节**上文**「关于 `执行/` 文件名里的三位前缀」一致（表建档时间线，不代表业务优先级）。  
4. **索引（必须）**：在 **[`index.md`](index.md)** **「### 执行」** 段按 **`NNN`** 升序 **补一行链接**；**更名或删除**任务单时同步 **改链或删链**。全文约定见 **下一节「全量索引维护（index.md）」**。

**任务单状态语种：** `执行/*.md` 文首 `status:` **只使用中文**（与 `00.项目任务清单.md` 所列五种取值**逐字一致**），不写 `todo` / `active` / `done` 等英文枚举。

## 「探查」目录：文件名编号与生成顺序

探查单落在 **`docs/项目计划/探查/`**。文件名 **`NNN-…`**：**三位十进制前缀**（从 **`001`** 起连续递增）+ **中文主题**（须满足总则 **[`AGENTS.md`](../../AGENTS.md)** §5 第 **14** 条「给人读的 Markdown 文件名须含中文」；允许如同 **`004-探查-process_lost_retry.md`** 这样在主题里保留必要英文标识）。

**`NNN` 表示什么：** 只表示该篇**进入 `探查/` 目录并采用当前命名体系的时间顺序**（建档/归档先后），**不表示**业务优先级，也**不与** `执行/` 的 **`NNN-`** 共用同一条取号链——执行单取号仍只盯 **`执行/`** 下现有 **`NNN-*.md`**。

**生成顺序（排序真值，用来决定谁算 001、002…）：**

1. **已纳入 Git 跟踪**：以对该路径 **`git log --diff-filter=A --follow -1`**（首次加入版本库的提交）的时间戳为准；**同一提交秒级同时加入多篇**时，按文件名片段 **`zh-CN` 区域设置下的字典序**做稳定次序，避免随缘排序。  
2. **尚未纳入 Git**（仅在工作区新建）：以操作系统记录的 **文件创建时间** 为准；若无可靠创建时间则用 **修改时间**；再与已追踪条目按时间戳合并排序（通常等价于「后进目录的在同类里排在后面」）。

**写单后维护（必须）：** 新开篇目须在 **[`index.md`](index.md)** **「### 探查」** 段按 **`001`→递增** **补一行链接**；**更名、批量改前缀或删除**时同步 **改链或删链**，并修补 **`docs/`** 树内指向旧文件名的引用（含 `` `探查/…` ``、`../探查/…`、`docs/项目计划/探查/…` 及无扩展名的路径写法）。全文约定见 **下一节「全量索引维护（index.md）」**。

## 「最佳实践」目录：文件名编号与生成顺序

最佳实践专文落在 **`docs/项目计划/最佳实践/`**。文件名 **`NNN-…`**：**三位十进制前缀**（从 **`001`** 起连续递增）+ **中文主题**（须满足总则 **[`AGENTS.md`](../../AGENTS.md)** §5 第 **14** 条「给人读的 Markdown 文件名须含中文」；允许在主题里保留必要英文标识，如 **`007-CodeBuddy-火山引擎端点与配额.md`**）。

**`NNN` 表示什么：** 只表示该篇**进入 `最佳实践/` 目录并采用当前命名体系的时间顺序**（建档/归档先后），**不表示**业务优先级，也**不与** **`执行/`**、**`探查/`** 的 **`NNN-`** 共用同一条取号链——取号时只盯 **`最佳实践/`** 下现有 **`NNN-*.md`**（不含 **`README.md`**）。

**生成顺序（排序真值，用来决定谁算 001、002…）：** 与 **「探查」目录**同一套规则——

1. **已纳入 Git 跟踪**：以对该路径 **`git log --diff-filter=A --follow -1`**（首次加入版本库的提交）的时间戳为准；**同一提交秒级同时加入多篇**时，按文件名片段 **`zh-CN` 区域设置下的字典序**做稳定次序。  
2. **尚未纳入 Git**（仅在工作区新建）：以操作系统记录的 **文件创建时间** 为准；若无可靠创建时间则用 **修改时间**；再与已追踪条目按时间戳合并排序。

**写单后维护（必须）：** 新开篇目须在 **[`index.md`](index.md)** **「### 最佳实践」** 段按 **`001`→递增** **补一行链接**（**[`README.md`](最佳实践/README.md)** 保持在分段**首行**）；**更名、批量改前缀或删除**时同步 **改链或删链**，并修补 **`docs/`** 树内指向旧文件名的引用（含 `` `最佳实践/…` ``、`../最佳实践/…`、`docs/项目计划/最佳实践/…`）。目录内归类与场景速查见 **[`最佳实践/README.md`](最佳实践/README.md)**。全文约定见 **下一节「全量索引维护（index.md）」**。

## 全量索引维护（`index.md`）

**[`index.md`](index.md)** 是本目录的**全量链接索引**（按分段列出 `执行/`、`探查/`、`最佳实践/`、`长期需求/`、`系统逻辑/`）。凡在以上子目录 **新建、更名或删除** 单篇 `.md`（即「写单子」或收口删稿），**必须**与正文改动**同一轮**更新 **`index.md`**，**禁止**只落文件、只改 `00` 主表却不补索引。

| 子目录 | 在 `index.md` 中的位置 | 操作要点 |
| --- | --- | --- |
| **`执行/`** | **「### 执行」** | 按文件名 **`NNN-`** **数值升序**插入或调整链接；删稿则删链。 |
| **`探查/`** | **「### 探查」** | **`README.md`** 置顶一行；其余专文按 **`001`→递增** 插入或调整；删稿则删链。 |
| **`最佳实践/`** | **「### 最佳实践」** | **`README.md`** 置顶一行；其余专文文件名 **`NNN-…`**，索引内按 **`NNN`** **数值升序**。命名、排序真值与写单后维护见上文 **「最佳实践」目录**及 **[`最佳实践/README.md`](最佳实践/README.md)**；删稿则删链。 |
| **`长期需求/`** | **「### 长期需求」** | 同上；删稿则删链。 |
| **`系统逻辑/`** | **「### 系统逻辑」** | 一般为 [`系统逻辑/README.md`](系统逻辑/README.md) 一行索引；正文增量落在 `探查/` / `执行/` 等，由 README 内表链入。 |

路径中带 **空格** 的链接，href 须按 **`index.md` 既有写法**只把空格编成 **`%20`**（勿整条路径过度编码），以保持可读与工具兼容。

## 入口

| 用途 | 文件 |
| --- | --- |
| 全量链接索引（写单子必同步维护） | [`index.md`](index.md)（规则见上文 **「全量索引维护」**） |
| 需求、开放问题、残留风险 | [`01.项目需求说明.md`](01.项目需求说明.md) |
| 任务状态、验收标准、规则 | [`00.项目任务清单.md`](00.项目任务清单.md) |
| 派工与执行记录 | [`执行/`](执行/) |
| 探查结论 | [`探查/README.md`](探查/README.md)（目录说明与归类索引）、[`探查/`](探查/)（命名 **`NNN-`** 见上文 **「探查」目录：文件名编号与生成顺序**） |
| 可复用打法 | [`最佳实践/README.md`](最佳实践/README.md)（目录说明与归类索引）、[`最佳实践/`](最佳实践/)（命名 **`NNN-`** 见上文 **「最佳实践」目录：文件名编号与生成顺序**） |
| 长期需求（跨工单、不设 sprint 截止） | [`长期需求/README.md`](长期需求/README.md) |
| 系统逻辑（运行时语义索引，链到代码与探查/执行） | [`系统逻辑/README.md`](系统逻辑/README.md) |
| **042～053 批次固定套路**（探查 / 设计 / 评审 / TDD / 回写） | [`最佳实践/016-实践-042至053编排开发收口套路.md`](最佳实践/016-实践-042至053编排开发收口套路.md) |
